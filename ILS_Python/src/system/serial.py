'''
Created on Sep 11, 2023

@author: phass
'''

def closeSerialPort(port):
    return

def configureSerialPort(port, bitRate, dataBits, handshake, hardwareFlowControl, parity, stopBits):
    return

def openSerialPort(port):
    return

def port(port, bitRate, dataBits, handshake, hardwareFlowControl, parity, stopBits):
    portManager = ""
    return portManager

def readBytes(port, numberOfBytes, timeout):
    bytes = ""
    return bytes

def readBytesAsString(port, numberOfBytes, timeout, encoding):
    txt = ""
    return txt

def readLine(port, timeout, encoding):
    txt = ""
    return txt

def readUntil(port, delimiter, includeDelimiter, timeout):
    txt = ""
    return txt

def sendBreak(port, millis):
    return

def write(port, toWrite, timeout, encoding):
    return

def writeBytes(port, toWrite):
    return