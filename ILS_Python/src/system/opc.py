'''
Created on Jun 7, 2015

@author: Pete
'''

def readValue(serverName, itemId):
    qv=""
    return qv

def readValues(serverName, itemIds):
    qvs=[]
    return qvs

# Returns the status of the write. Use returnValue.isGood() to check if the write succeeded.
def writeValue(serverName, itemId, value):
    quality = ""
    return quality

# Returns the status of the write. Use returnValue.isGood() to check if the write succeeded.
def writeValues(serverName, itemIds, values):
    qualities = []
    return qualities